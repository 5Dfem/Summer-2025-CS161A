Compile and run:
    g++ main.cpp && ./a.out && rm ./a.out

Compile and run with showing all errors:
    g++ -Wall main.cpp && ./a.out && rm ./a.out

Compile and run with test file:
    g++ main.cpp && ./a.out < [test file name] && rm ./a.out